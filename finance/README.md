# Finance SAM Plugin

Stock price and fundamentals data plugin using yfinance

This is a plugin for Solace Agent Mesh (SAM).

## Installation

Once the plugin is installed (e.g., from PyPI or a local wheel file):
```bash
sam plugin add <your-new-component-name> --plugin finance
```